import styled from 'styled-components';

const ListItemTitle = styled.p`
  margin:6px 0;
  margin-left:-10px;
  font-size: 12px;
    cursor:pointer;
    :hover {
      font-weight:bold;
    }
`;

export default ListItemTitle;
